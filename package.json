{
  "name": "micro-panel",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "sh -c \"trap 'kill 0' EXIT; (cd backend && mvn spring-boot:run) & (vite --host)\"",
    "dev:frontend": "vite --host",
    "dev:backend": "cd backend && mvn spring-boot:run",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.0",
    "typescript": "~5.9.3",
    "vite": "^7.2.2"
  }
}
