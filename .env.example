# Base URL used by the front-end when communicating with the media API.
# Defaults to /api which is proxied by Vite in dev/preview.
VITE_API_BASE_URL=/api

# Optional explicit proxy target for the Vite dev/preview server.
# This is useful when the API is hosted on another machine and does not
# expose CORS headers.
# VITE_PROXY_TARGET=http://10.41.40.130:1234
